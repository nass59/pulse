import { GoPath } from "@/components/go/go-path";

export const metadata = {
  title: "Go for a TypeScript engineer — Pulse docs",
  description:
    "A short, linear Go track for an engineer fluent in TypeScript and new to Go — the handful of ideas you need to read Pulse's chat gateway: errors as values, goroutines and channels, and the cgo-backed Kafka client.",
};

<GoPath />

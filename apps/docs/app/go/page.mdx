import { PillarOverview } from "@/components/docs/pillar-overview";

export const metadata = {
  title: "Go — Pulse docs",
  description:
    "The Go pillar: a linear path for an engineer fluent in TypeScript and new to Go — the ideas you need to read Pulse's chat gateway (errors as values, goroutines and channels, the cgo-backed Kafka client) — plus the WebSocket fan-out pattern the gateway exercises.",
};

<PillarOverview pillar="go" />

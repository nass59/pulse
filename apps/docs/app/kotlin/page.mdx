import { PillarOverview } from "@/components/docs/pillar-overview";

export const metadata = {
  title: "Kotlin — Pulse docs",
  description:
    "The Kotlin pillar: the analytics service (Kotlin on Kafka Streams) is designed but not yet built. A placeholder for the stream-processing lesson — windowed aggregates, state stores, and stream–table joins — coming when the service ships.",
};

<PillarOverview pillar="kotlin" />

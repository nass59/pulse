import { PillarOverview } from "@/components/docs/pillar-overview";

export const metadata = {
  title: "Distributed Systems — Pulse docs",
  description:
    "The architecture shelf: readings from Software Architecture: The Hard Parts (and the books after it) retold through Pulse — each chapter's trade-off mapped onto a decision this system actually made.",
};

<PillarOverview pillar="systems" />

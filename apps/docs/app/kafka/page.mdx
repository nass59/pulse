import { PillarOverview } from "@/components/docs/pillar-overview";

export const metadata = {
  title: "Kafka — Pulse docs",
  description:
    "The Kafka pillar: a linear path that teaches Kafka the technology from zero, plus the Pulse-specific concepts the build exercises — the log, partitions, consumer groups, KRaft, schema compatibility, the transactional outbox, and server-authored events.",
};

<PillarOverview pillar="kafka" />

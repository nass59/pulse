import { LearnPath } from "@/components/learn/learn-path";

export const metadata = {
  title: "Learn Kafka from zero — Pulse docs",
  description:
    "A short, linear track that teaches Kafka the technology to a senior engineer with no prior Kafka background — diagram-first, one idea per page.",
};

<LearnPath />

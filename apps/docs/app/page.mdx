import { FullBleed } from "@/components/docs/full-bleed";
import { EventRide } from "@/components/home/event-ride";
import { HomeSheet } from "@/components/home/home-sheet";
import { PillarPanels } from "@/components/home/pillar-panels";
import { RoadmapPhaseDrawers } from "@/components/home/roadmap/phase-drawers";
import { Tiers } from "@/components/home/tiers";
import { TitleBlock } from "@/components/home/title-block";

export const metadata = {
  title: "Pulse — learning Kafka by building the real thing",
  description:
    "A Twitch-style platform where every chat message and viewer count rides a real event-streaming backbone — TypeScript, Go and Kotlin around a Kafka log, built in public with every decision written down.",
};

<FullBleed>
  <HomeSheet>
    <EventRide />
    <PillarPanels />
    <RoadmapPhaseDrawers />
    <Tiers />
    <TitleBlock />
  </HomeSheet>
</FullBleed>

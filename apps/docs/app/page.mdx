import { FullBleed } from "@/components/docs/full-bleed";
import { FirehoseSection } from "@/components/home/firehose-section";
import { Hero } from "@/components/home/hero";
import { RoadmapPhaseDrawers } from "@/components/home/roadmap/phase-drawers";
import { Tiers } from "@/components/home/tiers";

export const metadata = {
  title: "Pulse — learn Kafka by building the system that needs it",
  description:
    "A friendly, diagram-first read of Pulse: a Twitch-style livestream platform built to learn Kafka and distributed-systems architecture, one lesson at a time.",
};

<FullBleed>
  <Hero />
</FullBleed>

<FirehoseSection />

<FullBleed>
  <RoadmapPhaseDrawers />
</FullBleed>

<FullBleed>
  <Tiers />
</FullBleed>

<Callout variant="note" title="This is a study artifact">
  These pages are the *friendly* read of Pulse — they translate decisions made
  and recorded elsewhere in the repo. They aim to teach, not to be the final
  word: when a page disagrees with the canonical write-ups, the write-ups win.
  Every page keeps a collapsed **Sources** trail at the bottom if you want to dig
  to the original reasoning.
</Callout>
